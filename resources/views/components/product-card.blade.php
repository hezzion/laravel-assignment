<div class="col-6">
    <div class="product-border">
        <img src="{{ asset($image) }}" alt="{{ $name }}" class="product-img">
        <p class="product-description">{{ $name }}</p>
    </div>
</div>



